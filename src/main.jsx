import React,{useCallback,useEffect,useMemo,useRef,useState}from"react";
import ReactDOM from"react-dom/client";
import{createAppKit,AppKitAccountButton,AppKitButton,useAppKit,useAppKitAccount,useAppKitNetwork}from"@reown/appkit/react";
import{WagmiAdapter}from"@reown/appkit-adapter-wagmi";
import{sepolia}from"@reown/appkit/networks";
import{QueryClient,QueryClientProvider}from"@tanstack/react-query";
import{WagmiProvider,useBalance,useSendTransaction,useWaitForTransactionReceipt}from"wagmi";
import{formatEther,isAddress,parseEther}from"viem";
import"./style.css";

const PROJECT_ID=import.meta.env.VITE_REOWN_PROJECT_ID||"ff3f925ac7d1161fbe3707bc77b3d9fe";
const SEPOLIA_ID=11155111,queryClient=new QueryClient(),networks=[sepolia];
const metadata={name:"CryptoPool PRO",description:"CryptoPool PRO — live crypto market and Ethereum Sepolia testnet",url:"https://huong83.github.io/CryptoPool-App/",icons:["https://huong83.github.io/CryptoPool-App/icon.svg"]};
const adapter=new WagmiAdapter({networks,projectId:PROJECT_ID,ssr:true});
createAppKit({adapters:[adapter],networks,projectId:PROJECT_ID,metadata,defaultNetwork:sepolia,allWallets:"SHOW",features:{analytics:false,email:true,socials:["google"],emailShowWallets:true},themeMode:"dark"});
const BINANCE_API="https://data-api.binance.vision/api/v3",BINANCE_WS="wss://stream.binance.com:9443/stream?streams=",COINGECKO_API="https://api.coingecko.com/api/v3",PAPRIKA_API="https://api.coinpaprika.com/v1";
const ASSETS=[
 {symbol:"BTC",name:"Bitcoin",binance:"btcusdt",cg:"bitcoin",tone:"orange"},{symbol:"ETH",name:"Ethereum",binance:"ethusdt",cg:"ethereum",tone:"purple"},{symbol:"SOL",name:"Solana",binance:"solusdt",cg:"solana",tone:"green"},{symbol:"BNB",name:"BNB",binance:"bnbusdt",cg:"binancecoin",tone:"gold"},{symbol:"LINK",name:"Chainlink",binance:"linkusdt",cg:"chainlink",tone:"indigo"},{symbol:"XRP",name:"XRP",binance:"xrpusdt",cg:"ripple",tone:"blue"},{symbol:"ADA",name:"Cardano",binance:"adausdt",cg:"cardano",tone:"cyan"},{symbol:"PI",name:"Pi Network",binance:null,cg:"pi-network",paprika:"pi2-pi-network",tone:"violet"},{symbol:"SDA",name:"SDA",binance:null,cg:null,tone:"slate"},{symbol:"UNI",name:"Uniswap",binance:"uniusdt",cg:"uniswap",tone:"pink"}
];
const INTERVALS=[{label:"1P",api:"1m",limit:120},{label:"15P",api:"15m",limit:96},{label:"1H",api:"1h",limit:72},{label:"4H",api:"4h",limit:72},{label:"12H",api:"4h",limit:72},{label:"1D",api:"1d",limit:60},{label:"7D",api:"1d",limit:7}];
const POOLS=ASSETS.map((asset,i)=>({asset,name:`${asset.name} Pool`,risk:i<3?"Core":i<7?"Balanced":"Growth"}));
function emptyMarket(a){return{...a,price:null,change:null,open:null,updatedAt:0,source:"WAITING"}}
function money(v){const n=Number(v);if(!Number.isFinite(n))return"—";if(n<.01)return n.toFixed(6);if(n<2)return n.toFixed(4);if(n<1000)return n.toLocaleString("en-US",{maximumFractionDigits:4});return n.toLocaleString("en-US",{maximumFractionDigits:2})}
function shorten(v){return v?`${v.slice(0,6)}…${v.slice(-4)}`:""}function timeLabel(ts){return ts?new Date(ts).toLocaleTimeString("vi-VN",{hour:"2-digit",minute:"2-digit",second:"2-digit"}):"—"}
function ConnectWallet({compact=false}){const{open}=useAppKit(),{isConnected}=useAppKitAccount();if(isConnected)return compact?<AppKitAccountButton balance="hide"/>:<AppKitButton/>;return <button className={compact?"wallet-connect compact":"wallet-connect"} onClick={()=>open({view:"Connect"})}>Kết nối ví</button>}
function useRealtimeMarket(){const[data,setData]=useState(()=>Object.fromEntries(ASSETS.map(a=>[a.symbol,emptyMarket(a)]))),[connected,setConnected]=useState(false),[lastUpdate,setLastUpdate]=useState(0),socketRef=useRef(null),retryRef=useRef(null),retryCount=useRef(0);const apply=useCallback((symbol,price,change,source,open=null)=>{const p=Number(price);if(!Number.isFinite(p)||p<=0)return;const c=Number(change);setData(prev=>({...prev,[symbol]:{...prev[symbol],price:p,change:Number.isFinite(c)?c:null,open:Number(open)>0?Number(open):prev[symbol].open,updatedAt:Date.now(),source}}));setLastUpdate(Date.now())},[]);
 useEffect(()=>{let alive=true;const connect=()=>{if(!alive)return;try{const streams=ASSETS.filter(a=>a.binance).map(a=>`${a.binance}@ticker`).join("/");const ws=new WebSocket(`${BINANCE_WS}${streams}`);socketRef.current=ws;ws.onopen=()=>{retryCount.current=0;alive&&setConnected(true)};ws.onmessage=e=>{try{const x=JSON.parse(e.data)?.data,a=ASSETS.find(v=>v.binance===x?.s?.toLowerCase());if(a)apply(a.symbol,x.c,x.P,"LIVE MARKET",x.o)}catch{}};ws.onerror=()=>{try{ws.close()}catch{}};ws.onclose=()=>{if(!alive)return;setConnected(false);retryRef.current=setTimeout(connect,Math.min(15000,2000*++retryCount.current))}}catch{setConnected(false);retryRef.current=setTimeout(connect,5000)}};connect();return()=>{alive=false;clearTimeout(retryRef.current);try{socketRef.current?.close()}catch{}}},[apply]);
 useEffect(()=>{let cancelled=false;const poll=async()=>{try{const symbols=ASSETS.filter(a=>a.binance).map(a=>a.binance.toUpperCase()),r=await fetch(`${BINANCE_API}/ticker/24hr?symbols=${encodeURIComponent(JSON.stringify(symbols))}`,{cache:"no-store"});if(!r.ok)throw Error();const rows=await r.json();if(!cancelled)rows.forEach(x=>{const a=ASSETS.find(v=>v.binance?.toUpperCase()===x.symbol);if(a)apply(a.symbol,x.lastPrice,x.priceChangePercent,"LIVE MARKET",x.openPrice)})}catch{}};poll();const t=setInterval(poll,15000);return()=>{cancelled=true;clearInterval(t)}},[apply]);
 useEffect(()=>{let cancelled=false;const loadPi=async()=>{try{let r=await fetch(`${COINGECKO_API}/simple/price?ids=pi-network&vs_currencies=usd&include_24hr_change=true`,{cache:"no-store"});if(r.ok){const x=await r.json(),p=x["pi-network"];if(p?.usd){if(!cancelled)apply("PI",p.usd,p.usd_24h_change,"COINGECKO");return}}}catch{}try{const r=await fetch(`${PAPRIKA_API}/tickers/pi2-pi-network?quotes=USD`,{cache:"no-store"});if(!r.ok)throw Error();const x=await r.json(),p=x?.quotes?.USD;if(!cancelled&&p?.price)apply("PI",p.price,p.percent_change_24h,"COINPAPRIKA")}catch{}};loadPi();const t=setInterval(loadPi,60000);return()=>{cancelled=true;clearInterval(t)}},[apply]);return{data,connected,lastUpdate}}
function MiniChart({points,large=false}){if(!points?.length)return <div className={large?"chart-empty large":"chart-empty"}>Chưa có dữ liệu biểu đồ</div>;const w=large?900:180,h=large?300:54,p=large?12:3,min=Math.min(...points),max=Math.max(...points),range=max-min||1,d=points.map((v,i)=>`${i?"L":"M"}${p+i*((w-p*2)/Math.max(points.length-1,1))},${h-p-((v-min)/range)*(h-p*2)}`).join(" ");return <svg className={large?"chart-large":"chart-mini"} viewBox={`0 0 ${w} ${h}`} preserveAspectRatio="none"><path d={d} fill="none" stroke="currentColor" strokeWidth={large?2.5:2} vectorEffect="non-scaling-stroke"/></svg>}
function AssetCard({asset,onOpen}){const available=Number.isFinite(Number(asset.price)),rising=Number(asset.change)>=0,points=asset.open&&asset.price?[Number(asset.open),Number(asset.price)]:[];return <button className="asset-card asset-click" onClick={()=>onOpen(asset)}><div className="asset-head"><div className={`asset-icon ${asset.tone}`}>{asset.symbol[0]}</div><div><strong>{asset.symbol}</strong><span>{asset.name}</span></div><span className={asset.change==null?"change muted":rising?"change positive":"change negative"}>{asset.change==null?"—":`${rising?"+":""}${Number(asset.change).toFixed(2)}%`}</span></div><div className="asset-price">{available?`$${money(asset.price)}`:"Đang tải…"}</div><MiniChart points={points}/><div className="asset-live"><span className={asset.source!=="WAITING"?"live-pulse":""}>{asset.source}</span><small>{timeLabel(asset.updatedAt)}</small></div></button>}
function Header({title,text,action}){return <div className="page-header"><div><div className="kicker">CRYPTOPool PRO</div><h1>{title}</h1><p>{text}</p></div>{action}</div>}
function useChart(asset,interval){const[points,setPoints]=useState([]),[loading,setLoading]=useState(false),[error,setError]=useState(false);useEffect(()=>{let cancelled=false;const cfg=INTERVALS.find(x=>x.label===interval)||INTERVALS[2];const load=async()=>{setLoading(true);setError(false);setPoints([]);try{if(asset.binance){const r=await fetch(`${BINANCE_API}/klines?symbol=${asset.binance.toUpperCase()}&interval=${cfg.api}&limit=${cfg.limit}`,{cache:"no-store"});if(!r.ok)throw Error();const rows=await r.json();if(!rows.length)throw Error();if(!cancelled)setPoints(rows.map(x=>Number(x[4])).filter(Number.isFinite))}else if(asset.cg){const days=cfg.label==="7D"?7:1,r=await fetch(`${COINGECKO_API}/coins/${asset.cg}/market_chart?vs_currency=usd&days=${days}`,{cache:"no-store"});if(!r.ok)throw Error();const j=await r.json(),rows=(j.prices||[]).map(x=>Number(x[1])).filter(Number.isFinite);if(!rows.length)throw Error();if(!cancelled)setPoints(rows.slice(-Math.max(cfg.limit,24)))}else throw Error()}catch{if(!cancelled){setError(true);setPoints([])}}finally{if(!cancelled)setLoading(false)}};load();return()=>{cancelled=true}},[asset.binance,asset.cg,interval]);return{points,loading,error}}
function ChartModal({asset,onClose}){const[interval,setIntervalValue]=useState("1H"),{points,loading,error}=useChart(asset,interval);useEffect(()=>{const f=e=>e.key==="Escape"&&onClose();addEventListener("keydown",f);return()=>removeEventListener("keydown",f)},[onClose]);const current=points.at(-1)||asset.price,first=points[0]||current,delta=first&&current?((current-first)/first)*100:null;return <div className="modal-backdrop" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className="chart-modal"><div className="modal-head"><div><div className="kicker">{asset.symbol} / USD</div><h2>{asset.name}</h2><div className="modal-price">{asset.price?`$${money(asset.price)}`:"Đang tải…"}</div></div><button className="icon-button" onClick={onClose}>×</button></div><div className="chart-tabs">{INTERVALS.map(v=><button key={v.label} className={interval===v.label?"active":""} onClick={()=>setIntervalValue(v.label)}>{v.label}</button>)}</div><div className="chart-wrap"><MiniChart points={points} large/>{loading&&<span className="chart-loading">Đang tải…</span>}</div><div className="chart-meta"><span>Khung {interval}</span><strong className={delta==null?"muted":delta>=0?"positive":"negative"}>{delta==null?"—":`${delta>=0?"+":""}${delta.toFixed(2)}%`}</strong><span>{error?"Nguồn dữ liệu chưa khả dụng":"LIVE MARKET DATA"}</span></div>{asset.symbol==="SDA"&&<div className="notice"><span>◉</span><div><strong>SDA chưa có nguồn giá được xác minh</strong><p>CryptoPool không tự tạo giá SDA. Tài sản vẫn được hiển thị để chờ nguồn thị trường chính thức.</p></div></div>}</section></div>}
function WalletBalance({address,asset="ETH"}){const{data}=useBalance({address,chainId:SEPOLIA_ID,query:{enabled:Boolean(address)&&asset==="ETH"}});if(!address)return <span className="muted">Chưa kết nối</span>;return asset==="ETH"?<strong>{data?`${Number(formatEther(data.value)).toFixed(6)} ETH`:"Đang đọc…"}</strong>:<span className="muted">Chưa cấu hình token</span>}
function Pools(){const[selected,setSelected]=useState(null),{address}=useAppKitAccount();return <div className="page"><Header title="Pools" text="10 pool theo tài sản. Yield chỉ bật khi có vault/strategy thật." action={<ConnectWallet/>}/><div className="pool-summary"><div><span>Assets</span><strong>10</strong></div><div><span>Wallet</span><strong>{address?shorten(address):"Chưa kết nối"}</strong></div><div><span>Yield</span><strong>ON-CHAIN ONLY</strong></div></div><div className="pool-list">{POOLS.map(p=><button key={p.asset.symbol} className={`pool-row ${selected?.asset.symbol===p.asset.symbol?"selected":""}`} onClick={()=>setSelected(p)}><div className={`asset-icon ${p.asset.tone}`}>{p.asset.symbol[0]}</div><div className="pool-row-main"><strong>{p.name}</strong><span>{p.asset.symbol} · {p.risk}</span></div><div className="pool-wallet"><small>Ví kết nối</small><WalletBalance address={address}/></div><div className="pool-apy"><small>APY thực</small><strong>Chưa có vault</strong></div><span>→</span></button>)}</div></div>}
function Wallet(){const{address,isConnected}=useAppKitAccount(),{chainId}=useAppKitNetwork(),{data,refetch}=useBalance({address,chainId:SEPOLIA_ID,query:{enabled:Boolean(address)}}),good=Number(chainId)===SEPOLIA_ID;return <div className="page"><Header title="Wallet" text="Ví non-custodial trên Ethereum Sepolia." action={<ConnectWallet/>}/><div className="wallet-page-card">{isConnected?<><div className="connected-banner"><span className="big-check">✓</span><div><strong>Ví đã kết nối</strong><small>{shorten(address)}</small></div></div><div className="wallet-grid"><div><span>Network</span><strong>{good?"Ethereum Sepolia":`Chain ${chainId??"—"}`}</strong></div><div><span>ETH balance</span><strong>{data?`${Number(formatEther(data.value)).toFixed(6)} ETH`:"Đang đọc…"}</strong></div><div><span>Security</span><strong>Không lưu seed phrase</strong></div></div><button className="ghost-button" onClick={()=>refetch()}>↻ Làm mới số dư</button></>:<div className="empty-wallet"><div className="wallet-symbol">◈</div><h2>Kết nối ví</h2><p>Chọn ví và xác nhận kết nối trong ứng dụng ví.</p><ConnectWallet/></div>}</div></div>}
function Trade(){const{address,isConnected}=useAppKitAccount(),{chainId}=useAppKitNetwork(),goodNetwork=Number(chainId)===SEPOLIA_ID,{data:senderBalance,refetch:refetchSender}=useBalance({address,chainId:SEPOLIA_ID,query:{enabled:Boolean(address)}}),[recipient,setRecipient]=useState(""),{data:recipientBalance,refetch:refetchRecipient}=useBalance({address:recipient,chainId:SEPOLIA_ID,query:{enabled:isAddress(recipient)}}),{data:hash,error:sendError,isPending,sendTransactionAsync}=useSendTransaction(),{isLoading:confirming,isSuccess:confirmed}=useWaitForTransactionReceipt({hash,confirmations:1}),[amount,setAmount]=useState(""),[message,setMessage]=useState("");const eth=senderBalance?Number(formatEther(senderBalance.value)):0,receiverEth=recipientBalance?Number(formatEther(recipientBalance.value)):null,valid=isAddress(recipient),amountNumber=Number(amount),enough=Number.isFinite(amountNumber)&&amountNumber>0&&amountNumber<eth;const send=async()=>{setMessage("");if(!isConnected||!goodNetwork||!valid||!enough||isPending||confirming)return;try{const h=await sendTransactionAsync({to:recipient,value:parseEther(amount),chainId:SEPOLIA_ID});setMessage("Giao dịch đã được gửi lên mạng Sepolia.");setTimeout(()=>{refetchSender();refetchRecipient()},1200);return h}catch(e){setMessage(e?.shortMessage||"Giao dịch bị hủy hoặc ví không phản hồi.")}};useEffect(()=>{if(confirmed){refetchSender();refetchRecipient();setMessage("Giao dịch đã được xác nhận trên blockchain.")}},[confirmed,refetchSender,refetchRecipient]);return <div className="page"><Header title="Trade" text="Gửi ETH trên Ethereum Sepolia. Người dùng tự ký giao dịch trong ví." action={<ConnectWallet/>}/><div className="trade-layout"><div className="trade-box dark-wallet-panel"><div className="trade-label">VÍ GỬI</div><div className="address-card"><span>Địa chỉ</span><strong>{address?shorten(address):"Chưa kết nối"}</strong></div><div className="balance-box"><div><small>Số dư hiện tại</small><strong>{senderBalance?`${eth.toFixed(6)} ETH`:"—"}</strong></div><button onClick={()=>refetchSender()} disabled={!address}>↻</button></div><div className="trade-label">SỐ LƯỢNG GỬI</div><input className="amount" inputMode="decimal" value={amount} onChange={e=>setAmount(e.target.value.replace(/[^0-9.]/g,""))} placeholder="0.001"/><div className="trade-label">VÍ NHẬN</div><div className="input-with-action"><input className="recipient-input large-address-input" value={recipient} onChange={e=>setRecipient(e.target.value.trim())} placeholder="0x…"/><button onClick={()=>setRecipient("")}>Xóa</button></div><div className="address-card receiver"><span>Số dư ví nhận</span><strong>{receiverEth==null?(valid?"Đang đọc…":"Nhập địa chỉ ví nhận"):`${receiverEth.toFixed(6)} ETH`}</strong><button onClick={()=>refetchRecipient()} disabled={!valid}>↻</button></div><button className="primary-button" disabled={!isConnected||!goodNetwork||!valid||!enough||isPending||confirming} onClick={send}>{confirming?"Đang chờ blockchain…":isPending?"Đang chờ ví ký…":!isConnected?"Kết nối ví":!goodNetwork?"Chuyển sang Sepolia":!valid?"Nhập địa chỉ nhận hợp lệ":!enough?"Không đủ ETH sau phí gas":"Mở ví để ký giao dịch"}</button>{(message||sendError)&&<div className={confirmed?"tx-success":"inline-warning"}>{message||sendError?.shortMessage}</div>}{hash&&<div className="tx-result"><div><b>Trạng thái giao dịch</b><span>{confirmed?"✓ Đã xác nhận":"Đang chờ xác nhận"}</span></div><a target="_blank" rel="noreferrer" href={`https://sepolia.etherscan.io/tx/${hash}`}>Kiểm tra trên Etherscan ↗</a></div>}</div><aside className="checks dark-wallet-panel"><h3>Gửi & nhận</h3><div className="check"><span className="check-good">✓</span><div><b>Ví gửi</b><small>{address?shorten(address):"Chưa kết nối"}</small></div></div><div className="check"><span className={valid?"check-good":"check-bad"}>{valid?"✓":"!"}</span><div><b>Ví nhận</b><small>{valid?shorten(recipient):"Chưa nhập địa chỉ"}</small></div></div><div className="check"><span className={goodNetwork?"check-good":"check-bad"}>{goodNetwork?"✓":"!"}</span><div><b>Network</b><small>{goodNetwork?"Ethereum Sepolia":"Cần Sepolia"}</small></div></div><div className="check"><span className={confirmed?"check-good":"check-bad"}>{confirmed?"✓":"…"}</span><div><b>Blockchain</b><small>{confirmed?"Đã xác nhận":hash?"Đang chờ xác nhận":"Chưa có giao dịch"}</small></div></div></aside></div></div>}
function Dashboard({market,go,openAsset}){const{address}=useAppKitAccount();return <div className="page"><section className="hero-card-main"><div className="hero-copy"><div className="kicker"><span className="live-dot"/> LIVE MARKET</div><h1>CryptoPool PRO<br/><em>tài sản số chuyên nghiệp.</em></h1><p>Theo dõi 10 tài sản với dữ liệu thị trường trực tiếp và biểu đồ đa khung thời gian. Ví hoạt động trên Ethereum Sepolia testnet.</p><div className="hero-actions"><ConnectWallet/><button className="ghost-button" onClick={()=>go("markets")}>Xem thị trường →</button></div><div className="trust-row"><span>✓ Non-custodial</span><span>✓ 10 assets</span><span>✓ Testnet only</span></div></div><div className="hero-visual"><div className="orb orb-a"/><div className="orb orb-b"/><div className="portfolio-float"><span>LIVE MARKET</span><strong>{market.connected?"CONNECTED":"CONNECTING"}</strong><small>Real-time market stream</small></div></div></section><section className="stats-row"><div><span>Market feed</span><strong>{market.connected?"LIVE MARKET":"Đang kết nối"}</strong></div><div><span>Assets tracked</span><strong>10 <small>crypto assets</small></strong></div><div><span>Wallet</span><strong>{address?shorten(address):"Not connected"}</strong></div></section><div className="section-title"><h2>10 tài sản</h2><button onClick={()=>go("markets")}>Xem tất cả →</button></div><div className="asset-grid">{Object.values(market.data).map(a=><AssetCard key={a.symbol} asset={a} onOpen={openAsset}/>)}</div></div>}
function Markets({market,openAsset}){const[q,setQ]=useState(""),list=Object.values(market.data).filter(a=>`${a.symbol} ${a.name}`.toLowerCase().includes(q.toLowerCase()));return <div className="page"><Header title="Tài sản crypto" text="10 tài sản · giá trực tiếp · SDA chỉ hiển thị khi có nguồn được xác minh." action={<ConnectWallet/>}/><div className="toolbar"><label className="search"><span>⌕</span><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Tìm BTC, ETH…"/></label><span className={market.connected?"feed-ok":"muted"}>{market.connected?"● LIVE MARKET":"○ CONNECTING"}</span></div><div className="asset-grid markets-grid">{list.map(a=><AssetCard key={a.symbol} asset={a} onOpen={openAsset}/>)}</div></div>}
function Portfolio({market}){const{address}=useAppKitAccount();return <div className="page"><Header title="Portfolio" text="Theo dõi thị trường và số dư ví on-chain." action={<ConnectWallet/>}/><div className="portfolio-hero"><span>LIVE MARKET</span><strong>{market.connected?"CONNECTED":"CONNECTING"}</strong><small>{address?`Ví ${shorten(address)} trên Sepolia`:"Kết nối ví để xem số dư on-chain"}</small></div><div className="portfolio-grid"><div><span>ETH wallet</span><strong><WalletBalance address={address}/></strong></div><div><span>Assets tracked</span><strong>10</strong></div><div><span>Pool yield</span><strong>ON-CHAIN ONLY</strong></div></div></div>}
function App(){const[page,setPage]=useState("dashboard"),[selected,setSelected]=useState(null),market=useRealtimeMarket(),go=useCallback(p=>{setPage(p);scrollTo({top:0,behavior:"smooth"})},[]),nav=useMemo(()=>[{id:"dashboard",icon:"⌂",label:"Dashboard"},{id:"markets",icon:"◌",label:"Markets"},{id:"pools",icon:"◈",label:"Pools"},{id:"portfolio",icon:"▣",label:"Portfolio"},{id:"trade",icon:"↗",label:"Trade"},{id:"wallet",icon:"◎",label:"Wallet"}],[]);return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">C</div><div><strong>CryptoPool</strong><span>PRO · WEB3 / DEFI</span></div></div><span className="nav-label">PLATFORM</span><nav>{nav.map(n=><button key={n.id} className={page===n.id?"active":""} onClick={()=>go(n.id)}><span>{n.icon}</span>{n.label}</button>)}</nav><div className="sidebar-bottom"><div className="wallet-mini"><div className="wallet-mini-top">● {market.connected?"Live Market":"Market connecting"}</div><strong>Ethereum Sepolia</strong><p>Testnet environment</p><span className="network-tag good">10 assets</span></div></div></aside><main><header className="topbar"><div className="mobile-brand"><div className="brand-mark">C</div><div><strong>CryptoPool PRO</strong><span>WEB3 / DEFI</span></div></div><span className="network-chip">● {market.connected?"LIVE MARKET":"CONNECTING"}</span><ConnectWallet compact/></header>{page==="dashboard"&&<Dashboard market={market} go={go} openAsset={setSelected}/>} {page==="markets"&&<Markets market={market} openAsset={setSelected}/>} {page==="pools"&&<Pools/>}{page==="portfolio"&&<Portfolio market={market}/>} {page==="trade"&&<Trade/>}{page==="wallet"&&<Wallet/>}</main>{selected&&<ChartModal asset={selected} onClose={()=>setSelected(null)}/>}<nav className="mobile-nav">{nav.map(n=><button key={n.id} className={page===n.id?"active":""} onClick={()=>go(n.id)}><span>{n.icon}</span><small>{n.label}</small></button>)}</nav></div>}
ReactDOM.createRoot(document.getElementById("root")).render(<WagmiProvider config={adapter.wagmiConfig}><QueryClientProvider client={queryClient}><App/></QueryClientProvider></WagmiProvider>);
